# 项目计划与展望

本文档旨在记录项目的当前完成状态，并为未来的功能迭代提供一个清晰的路线图。

## 已完成功能 (截至当前)

### 后端
- **核心架构**: 基于 Spring Boot 3 和 MyBatis-Plus 的稳定后端服务。
- **数据库**: 使用 PostgreSQL，并通过 `schema.sql` 实现启动时自动建表。
- **认证与授权**:
  - 集成 Apache Shiro，实现了完整的基于角色的权限控制（RBAC）。
  - 自定义了认证过滤器，完美适配前后端分离的 JSON API 交互模式。
  - 实现了 `admin` 和 `manager` 两种角色的数据隔离。
- **核心业务模块**:
  - **用户管理**: 提供增删改查及重置密码接口。
  - **店铺管理**: 提供增删改查及分配店长接口。
  - **座位/菜品/订单管理**: 提供完整的增删改查接口，并与店铺系统深度集成。
  - **文件上传**: 提供了通用的图片上传接口。
- **API规范**:
  - 所有 API 均返回统一的 `ApiResponse` JSON 格式。
  - 实现了全局异常处理器，确保了API的健壮性。

### 前端
- **核心架构**: 基于 Vite 和 Vue 3 的现代化前端工程。
- **UI**: 使用 Naive UI 构建了美观且响应式的后台管理界面。
- **状态管理**:
  - 实现了轻量级的全局状态管理，用于存储用户信息和权限。
  - 实现了完善的登录状态失效处理机制。
- **权限控制**:
  - 实现了菜单的动态渲染，用户只能看到自己有权访问的模块。
  - 通过路由守卫，防止用户通过 URL 直接访问未授权的页面。
- **核心业务模块**:
  - **用户管理**: 实现了功能完备的用户管理界面。
  - **店铺管理**: 实现了功能完备的店铺管理及店长分配界面。
  - **座位/菜品管理**: 实现了与店铺联动的管理界面，支持图片上传。
  - **订单管理**: 实现了功能完备的订单创建和管理界面。

## 下一步工作计划

### 优先级 - 高
1.  **客户系统与小程序端**:
    - 设计客户（Customer）实体和相关 API。
    - 开发小程序前端，实现客户扫码、浏览菜单、下单、支付等核心流程。
2.  **后厨看板**:
    - 开发一个专门的后厨视图，实时显示“准备中”的订单。
    - 实现订单状态的实时更新（可以考虑使用 WebSocket）。
3.  **数据看板 (Dashboard)**:
    - 开发一个数据可视化首页，展示关键业务指标，如今日流水、订单量、热门菜品等。
    - 后端需要提供相应的数据聚合查询 API。

### 优先级 - 中
1.  **预约功能**:
    - 完善预约相关的业务逻辑和 API。
    - 在后台管理系统中添加入口，允许店员处理线上和线下的预约请求。
2.  **支付集成**:
    - 集成微信支付或支付宝支付。
3.  **更细粒度的权限控制**:
    - 在前端页面上，根据权限动态禁用或隐藏“创建”、“删除”等具体的操作按钮。

### 长期规划
1.  **库存管理**: 为菜品添加库存字段，并在下单时进行校验。
2.  **会员系统**: 引入客户积分、优惠券等功能。
3.  **多语言支持**: 为系统添加国际化支持。
